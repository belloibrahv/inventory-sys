#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
IMAGE="${ATOMS_PHP_IMAGE:-wordpress:cli-php8.2}"
exec docker run --rm \
  --user "$(id -u):$(id -g)" \
  -e HOME=/tmp \
  -v "$ROOT":/app \
  -w /app \
  --entrypoint php \
  "$IMAGE" \
  "$@"
